# Copy to .env and use as-is or adjust. Do not commit .env if it contains real secrets.

# --- Database (used by writer script and app) ---
# Local: Host=localhost, Port=5432, Database=Experiments, Username=postgres
DATABASE_URL="postgresql://postgres:Hello123!!@localhost:5432/Experiments"

# --- Writer (scripts/sync_swagger_to_db.py) ---
SWAGGER_URL=https://app-api-flytel-e2ehbed2hng3g6hj.norwayeast-01.azurewebsites.net/swagger/v1/swagger.json
SOURCE_NAME=Flytel
# BASE_URL=https://example.com

# --- App (app.py when using external API tool) ---
API_SOURCE_NAME=Flytel
# Or by id: API_SOURCE_ID=1
EXTERNAL_API_BEARER_TOKEN=your_jwt_or_api_key_here

# --- Operation selection: static | ollama | openai ---
# static = Ollama picks from tool list (keyword fallback if empty). ollama = Ollama picks operation (no API key). openai = OpenAI picks (needs OPENAI_API_KEY).
EXECUTION_METHOD=ollama

# --- Required only when EXECUTION_METHOD=openai ---
# OPENAI_API_KEY=sk-...
